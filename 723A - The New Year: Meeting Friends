#include<bits/stdc++.h>
using namespace std;
int main(){
	int num[3],max = 0,min = INT_MAX;
	
	for(int i=0; i < 3;i++){
		cin >> num[i];		
	}
	sort(num,num+3);
	cout << num[2] - num[1] + num[1] - num[0];
	return 0;
}
